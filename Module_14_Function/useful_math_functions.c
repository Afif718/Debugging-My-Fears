#include<stdio.h>
#include<math.h>

int main(){

    // int ans= ceil(4.3); //upper bound
    //int ans= floor(4.3); //lower bound
    // int ans= round(4.3); //round the number
    //int ans= round(4.5); //round the number
    //int ans= sqrt(25); //sqare root
    //int ans= sqrt(16); //sqare root
    // int ans = pow(3,2); // 3 to the power 2 = 9
    // int ans = abs(10); //it will print 10
    int ans = abs(-10); // it will print the positive 10

    printf("%d", ans);
    return 0;
}